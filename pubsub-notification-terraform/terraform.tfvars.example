project_id = "your-gcp-project-id"
region     = "us-central1"

# Optional: Override defaults
# bucket_name = "test-dp-gcspubsub-bucket"
# topic_name  = "test-dp-gcspubsub-bucket"
# force_destroy = false
# enable_versioning = false

labels = {
  environment = "test"
  managed_by  = "terraform"
}

# Cross-project consumer configuration (optional)
# Enable this to create a subscription in a different project
# create_cross_project_subscription = true
# consumer_project_id = "your-consumer-project-id"

